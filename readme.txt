Because this project uses Flask from Python, images are placed in a separate /static folder and they CANNOT be accessed directly from html files.
So, to properly see them "in action", please run application using:

python main.py

And by accessing the website by going to http://127.0.0.1:5000